---
const { data } = Astro.props;
---

<ul>
  <li>
    {
      data.child ? (
        <a href={data.href}>{data.title}</a>
      ) : (
        <span>{data.title}</span>
      )
    }
    {data.child && <Astro.self data={data.child} />}
  </li>
</ul>

<style>
  ul {
    text-align: left;
    list-style-type: "» ";
    padding-left: 1em;
    padding-top: 0;
    margin-top: 0;
    line-height: 1.3;
  }

  ul ul {
    padding-left: 1rem;
  }

  li {
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
  }

  span {
    font-weight: 500;
    color: #1c425e;
  }
</style>
